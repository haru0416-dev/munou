# 人工無脳君 設計書

draft v0.1 — 2026-08-19  
v0.1 実装メモ — 同日  
v0.1.1 — 候補プール（トリガー / 検索 / マルコフ / エコー）  
v0.1.2 — 対話中のランダム吸収 (`p_learn`)  
v0.1.3 — 観察窓が本線。アダプタ（伺か / Misskey / 常駐）は劣後  
v0.1.4 — 数理: 次数補間・Witten-Bell・帯域ヒンジ・Boltzmann slip・最長一致は連続部分列  
v0.1.5 — LLM エコシステムの**概念**を閉じた構造へ逆輸入。API・配布重み・モデルハブは使わない  
v0.1.6 — 閉じた雑談ログを大量に複製して規模を試す。検索は直近 `n_retrieve_scan` 件  
v0.1.8 — 育った語彙で 30ms 予算を守る。分布を「疎な明示 id + 尾部スカラー×unigram」で持ち、|V| の実体化を生成ステップから外す（分布同一・応答列は変わる）  
v0.1.9 — 監査の修正。`Engine::open` を Θ(N) に（再生中の merge を止め最後に一回）、話題窓を live/replay で一致（user のみ）、KN の continuation / count-of-counts を buf 込みで即時更新、「ー」が CJK 隣接クラスを継承、trigger 埋め込みをロード時キャッシュ、fsync をターン 1 回に、`l_max` の 8 上限クランプを廃止  
v0.1.10 — 対話実測からの調整。自己反復の抑制（直近 3 発話の完全一致をプールから除外 + 窓 `self_window` の文字 LCS 減点 `self_penalty`）、生成候補の先頭句読点トリム、rote 減点の分母を min(候補長, 入力長) に、fabricate の表層追加を ASCII 数字からかな造語に  
v0.1.16 — メモリ面とコールドスタート（応答列変更を宣言。転写 diff は実測 0）。**EntropyModel の表現圧縮**（文脈 ≤6 文字を u128 キー 1 本に、後続 ≤3 対はインライン・あふれはソート済み Vec = エントロピーの加算順が正準化）、対記憶/検索窓の容量返却、wprefix の u32/u64 二段化、**file-backed ではレコード列を常駐させない**（ファイルが真実源。270万レコード ≈ 450MB が消える）、**スナップショット**（再生同値状態の純キャッシュ。鍵 = 改訂番号+パラメータ+ログの長さと FNV。open がフル構築後に書き、chat 終了時に更新。snapshot.bin の削除は常に安全）。実測は verify.md v0.1.16  
v0.1.15 — 体験層（応答列変更を宣言して実施）。解析した既存の非LLM対話製品から機構だけを翻訳して逆輸入: **関心**（熱/根の二重時定数のチャンク重み。減衰は壁時計でなく**ログ位置時計**）、**日和**（(seed, 前レコードの日) の決定的な日替わり係数。特殊日和は2日続けない）、**合いの手**（自ログの短発話を頻度分布のまま収穫し、返答の前に一行。表示のみ・非吸収）、**聞きかじり**（2発話未満の語はアンカーにしない。「口をつく」ターンでだけ放出）、**節目 / 初語 / よそよそしさ**（digest 交差から導出）、**あゆみ**（/ayumi の一枚記録）。すべてログ＋シードの純関数で、応答は (log, seed, input) の関数のまま  
v0.1.16 — 再開時の乱数位置を bot レコードへ保存。ChaCha8 の 68-bit word offset を JSON の u64 2語に分け、`Engine::open` が最後の保存位置へ seek する。旧ログは従来どおり位置0から始まるが、最初の新形式ターン以後は live と reopen の次応答が一致する
v0.1.14 — 依存の全面更新（応答列変更を宣言して実施）。rand_chacha 0.3→0.10、clap 4.5→4.6、MSRV 1.83→1.85。**`rand` 本体は依存から削除**し、一様サンプリング2関数（f64=上位53bit / 整数=Lemire 棄却法）を `rng.rs` に自前実装 — 応答列契約を ChaCha8 生ストリーム＋自前関数に係留し、今回を「外部要因の最後の応答列破壊」にする。副産物として getrandom/libc/wasi/cfg-if の OS 乱数系が依存木から消え、ロックは 45→40 crate  
v0.1.13 — バグ狩り（独立3係+ランダム化差分+反証）で確定した9件を修正。**デコードは SEP でも停止**（SEP を跨ぐと発話頭分布へ切り替わり無関係な冒頭が接がれ、吸収されるとストア境界不変条件と live/再開の再現性が壊れていた）、adapt 置換の全重複除去、Exclusive モードへの Adapt 漏れ遮断、アンカー生成の max_gen_len 厳守、learned フラグを実際に吸収した側だけに、digest/eval は追記成功後に、破断行の改行修復、n_adapt=0 で対記憶を完全停止、self_penalty=0 で反復抑制を完全停止  
v0.1.12 — 一般解ではない最適化。この系の不変条件（閉ループの発話反復・replay 中のトークナイザ凍結・照合パターンが発話境界を跨げない・候補の同一文脈分岐・p_learn 非吸収ターン）だけを使った同値変換: 重複一括学習、replay tokenize/embed メモ、観察窓の増分ダイジェスト、分布フリーズのターン跨ぎキャッシュ、**dedup ストア**（distinct 発話×多重度の重み付き SA。反復ログでは SA が distinct 分だけになり、繰り返し発話の吸収は SA に触れない）。出力は転写 diff で不変（buf 表示の意味のみ変更）  
v0.1.11 — 同等物調査からの逆輸入。**Adapt** ソース（市川宙 RubyKaigi2006 の機構の写し: 学習済みの「入力→返答」対から、いまの入力に似た対の返答をチャンク置換して提案 + 過去のユーザー発言そのものを話題で引用（役割反転の軽量版））、**アンカー双方向生成**（Hutchens & Alder 1998 の写し: 逆順ストリームの双子ストアで、入力中の最レアな既知チャンクから左右両方向へ生成。候補枠の半分）、**surprise 記録**（候補ごとの平均 −ln p を /why に出す。`surprise_weight` は実験用で既定 0）

本文は元ドラフト。末尾にこのリポジトリで固定した決定だけを足す。

## 1. 目的と思想

LLMを使わず、理解せずに会話が成立する対話プログラムを現代の道具立てで設計し直す。目標は賢さではなく、次の四つの性質。

1. **説明可能** — すべての応答について「なぜこれが出たか」を生成チェーンと選択スコアで完全に追跡できる
2. **育てられる** — 語彙・遷移・記憶がユーザー可視のデータ構造であり、会話ログから育つ過程が観察できる
3. **閉じている** — 外部知識を持たない。知っていることはすべて自分の会話ログ由来
4. **ズレる** — 文脈からの適度な逸脱を欠陥でなく仕様として設計する

副目的として、トークナイザー・n-gramストア・サンプリングの各層を数理とデータ構造の遊び場にする。個人ログ規模(〜数百万トークン)では速度は本来問題にならないため、高速化は必要性でなく楽しみとして行うことを明記しておく。

## 2. 制約

- LLM(生成用言語モデル)は使わない
- embedding は**選択**にのみ使う。生成には使わない
- ミニモデルは**分類器**としてのみ許可(対話行為・感情極性など)
- 学習データは自分の会話ログのみ。事前学習済み生成モデル・外部コーパスは禁止
  - 例外: embedding モデルと形態素辞書は既製を許可(生成に関与しないため)

### 2.1 決定済みの前提

- 実装スタック: **Rust**
- インターフェース: 当面 **CLI のみ**。**観察窓が本線**（ゲージは既存の発話・吸収・語彙・帯域・暗記・ズレ）。エンジンはインターフェース非依存の crate。CLI はその最初の面。**伺か SHIORI / Misskey / 常駐アダプタは後段（劣後）**
- 参照実装は探索の下限であり、設計空間を縛らない。各コンポーネントはゼロから設計する案を常に比較対象に含める

### 2.2 非機能要件(厳しめに設定)

速度・省メモリは必要性からでなく規律として課す。満たせない場合は設計を疑う。

| 項目 | 要件 |
|---|---|
| 応答レイテンシ | 入力→出力 p99 ≤ 30ms(embed 推論込み)。embed を除くエンジン部は p99 ≤ 2ms |
| トークナイズ速度 | ≥ 50MB/s(シングルスレッド、実測プレーンテキスト) |
| インデックス再構築 | 10^7 トークンの SA 全再構築 ≤ 2s |
| 常駐メモリ | コーパス 10^7 トークン時に ≤ 256MB |
| 起動 | コールドスタート ≤ 100ms(インデックスは mmap で遅延ロード) |
| 再現性 | 同一ログ+同一シードで応答列が完全一致(決定的)。ズレ注入も擬似乱数で再現可能 |
| 依存 | 実行時の外部プロセス・外部サービス依存なし。単一バイナリで動く |
| 耐障害 | 会話ログは append-only。プロセス強制終了でログを失わない(インデックスは再構築で復元可能) |
| 割り当て規律 | 応答ホットパスでのヒープ確保ゼロを目標(アリーナ/事前確保) |

## 3. 全体アーキテクチャ

生成と選択の分業が中核。**複数の手段が候補を出し、選択器が一本を選ぶ**。v0.1 はトリガー照合が当たるとマルコフを止める XOR で、空コーパスではユーザー文をバックオフに使っていた。それはおうむ返しを設計に埋め込んでいた。最終形はハイブリッドの候補プールである。

```
入力発話
   │
   ▼
[トークナイザー] ── 形態素列 → 文節チャンク列
   │
   ├─→ [対話行為分類器] 質問/相槌/呼びかけ… (任意・後段)
   │
   ▼
[話題トラッカー] 直近k発話の embedding 移動平均 = 話題ベクトル t
   │
   ├─→ [トリガー]     辞書応答
   ├─→ [検索]         過去の自分の発話（ログ由来）
   ├─→ [マルコフ]     可変長 n-gram の組み換え
   └─→ [エコー]       入力の再掲（明示。黙ってのフォールバックではない）
          │
          ▼
     [候補プール]
          │
          ▼
[選択器] 話題余弦 ± ソース事前 − 連続部分列 − 帯域ヒンジ
   │      ★ズレ注入: 確率 p で 2 位以下を Boltzmann サンプル
   ▼
[記憶] 発話をログへ追記。確率 p_learn でコーパスへ吸収（トークナイザ・SA・検索）
   │
   ▼
[観察窓] 吸収 / 語彙 / 帯域 / 暗記 / ズレ。会話より「育つ様子」が製品
   │
   ▼
出力
```

`--mix exclusive` は v0.1 の XOR を残す。既定は `--mix pool`。

## 4. コンポーネント設計

### 4.1 トークナイザー(自作対象)

既存二方式に限定せず、ゼロから設計する第三案を対等に置く。比較軸だけ固定する。

| 軸 | ラティス+Viterbi | 点予測 | ゼロ設計(教師なし/タスク駆動) |
|---|---|---|---|
| 構造 | 辞書引き+最小コスト経路 | 各文字境界を分類器で判定 | 統計量から境界を直接導出 |
| 速度の主戦場 | トライ/FST、接続表のキャッシュ効率 | 特徴量抽出、線形分類器の内積 | 統計量の前計算とルックアップ |
| 隠れコスト | 辞書メンテが重い | 学習データ作りが要る | 品詞が出ない。分割品質の保証がない |
| 品詞 | 自然に取れる | 追加タスクになる | 別途設計するか、不要にする |
| 参照点 | 既存のラティス型 Rust 実装 | 既存の点予測型 Rust 実装 | 分岐エントロピー法、accessor variety 等 |

ゼロ設計の核になるのは二つの問い。

- **教師なし境界検出**: 文字 n-gram の**双方向**分岐エントロピー（境界の左右で次/前の文字の多様性が跳ねる位置を境界とみなす）は辞書も教師データも要らず、自分のログだけで完結する。制約2(閉じている)と最も整合的
- **タスク駆動分割**: そもそも言語学的に正しい分割は目的ではない。マルコフの単位として最適な分割 — 生成品質(§6 の指標)を直接最大化する分割 — を探索対象にできる。トークナイザーを独立部品でなく、生成器と結合した最適化問題として扱う道

文節単位マルコフをやるには品詞相当の情報が要る。ゼロ設計を選ぶ場合、「文節」に固執せず統計的チャンクで代替できるかも含めて実験する。

出力は二層: 形態素列(学習・分類用)と文節チャンク列(マルコフの単位)。全トークンは interning して u32 ID に落とす。

### 4.2 コーパスストア

会話ログ全体を u32 ID の一本の配列として保持し、その上に接尾辞配列(SA-IS で O(n) 構築)を張る。

- 可変長 n-gram: 固定次数の HashMap を持たず、任意長の文脈を SA 上の二分探索で引く。次数固定による丸暗記問題が構造ごと消える
- 更新: incremental SA は難しいので世代制にする。新規発話は小さな HashMap バッファに置き、閾値超過時(または夜間)に本体へマージして SA を全再構築。数百万トークンなら再構築は一瞬
- 生成時は本体 SA とバッファの二段引き

### 4.3 マルコフ生成器

- 単位: 文節チャンク。文法的破綻を抑えつつ意味のズレを残す
- 次数: 可変長。接尾辞次数 1..n を短い順に補間する。`f_min` は説明用の「効いている次数」であり、確率は全次数を混ぜる（長い一致だけ見て unigram に飛ばさない）
- 平滑化: 既定は Witten-Bell（`λ = N/(N+T)`）。`--smoothing kn` で modified Kneser-Ney（Chen-Goodman の D1/D2/D3+）。いずれも下位分布への補間。既製の LM ツールチェーンは使わない
- サンプリング: 分布ごとに alias 法を前計算し O(1) 抽選。温度パラメータで分布を変形
- 停止: 文末トークンの明示 + 最大長ガード
- 疎な文脈だけ skip-gram（直前を一つ飛ばした次数）と recency cache を混ぜる

### 4.4 選択器

- 候補 N 本を embedding し、話題ベクトル t との内積でランク。**帯域ヒンジ**で [lo, hi] の外を減点する（余弦最大化は §6 の帯域目標と衝突するため）
- 丸暗記減点は入力との **token 連続部分列**（部分列 LCS は組み換えまで減点してしまう）
- 検索提案は MMR（類似 − 冗長）でクローンを減らす
- この規模では ANN(HNSW)は不要。SIMD の総当たり内積で足りる。埋め込みは i8 量子化を検討
- **ズレ注入**: 確率 p_slip で 2 位以下を Boltzmann（`τ_slip`）サンプル。トリガー閾値も意図的に緩く保つ。ズレはパラメータであって欠陥ではない

### 4.5 分類器(任意・後段)

対話行為(質問/相槌/挨拶/呼びかけ)と感情極性の小分類器。応答プールの切り替えに使う。線形モデルか数十MB以下の蒸留モデル。v1 では省略可。

### 4.6 記憶

- 短期: 話題ベクトル(直近 k 発話の移動平均)
- 長期: 発話ログ本体 + embedding index。「前に言ったこと」の検索に使う
- 固有記憶(名前・好み等の key-value)は v2 以降の検討事項

## 5. パラメータ一覧(調整対象)

| 名前 | 意味 | 初期値の目安 |
|---|---|---|
| N_cand | マルコフ候補数 | 10 |
| τ_gen | 生成温度 | 1.0 |
| L_max | 文脈最長一致の上限 | 8 チャンク |
| f_min | 説明用の最長サポート次数。確率は全次数を補間 | 3 |
| k_topic | 話題移動平均の窓 | 5 発話 |
| θ_trig | トリガー類似度閾値 | 意図的に緩く |
| p_slip | ズレ注入率 | 0.15 |
| τ_slip | 2 位以下の Boltzmann 温度 | 0.45 |
| p_learn | 対話ターンをコーパスへ吸収する確率。ログは毎回残す | 0.35 |
| mix | 候補の混ぜ方 `pool` / `exclusive` | pool |
| n_retrieve | プールに入れる過去ボット発話 | 4 |
| mmr_lambda | 検索 MMR の λ | 0.75 |
| lambda_skip | 疎な文脈での skip-gram 混合 | 0.12 |
| lambda_cache | 疎な文脈での recency cache 混合 | 0.10 |
| ppm_exclude | Witten-Bell に PPM-C 除外を足す。KN は常に除外 | false |
| echo_penalty | エコーの余弦減点 | 0.25 |
| rote_penalty | 入力との連続部分列減点。分母は min(候補長, 入力長) | 0.50 |
| self_penalty | 直近の自分の発話との文字 LCS 減点（反復ループ抑制） | 0.60 |
| self_window | 自己反復を見る直近 bot 発話数。完全一致は直近 3 件を候補から除外 | 8 |
| n_adapt | Adapt 提案数。1=置換返答、2=+ユーザー発言の引用 | 2 |
| adapt_penalty | Adapt の減点（改変コピーは Markov と Retrieve の中間） | 0.10 |
| bidir | アンカー双方向生成（逆順双子ストアを持つ。ストア側のメモリ・merge が約2倍） | true |
| surprise_weight | 選択への surprise 加点（実験用。0 = /why 表示のみ） | 0.0 |
| band_penalty | 帯域外ヒンジの重み | 0.50 |
| trigger_bonus | トリガー候補の加点 | 0.10 |
| trigger_match_weight | パターン一致度をトリガー得点に乗せる | 1.0 |
| retrieve_penalty | 検索候補の減点（丸写しより組み換えを少し優先） | 0.20 |

## 6. 評価 — 未解決の中核問題

perplexity 最小化を目的関数にするとログ再現に向かい、無脳らしさが死ぬ。面白さの定式化が本プロジェクト最大の open problem。候補となる代理指標:

- 文脈類似度の**帯域**目標: 高すぎず低すぎない範囲 [a, b] に収まった応答の率
- 新規性: 生成文とログ既存文の**最長連続一致**長(丸暗記検出)
- 破綻率: 文法破綻の人手アノテーション(小規模でよい)

当面は自動指標+自分の主観ログの併用とし、目的関数の設計自体を実験対象として扱う。

## 7. 未決事項

- 実装言語(Rust が既存スタックと親和的だが未決定)
- インターフェース。観察窓 + CLI が本線。伺か / Misskey / デスクトップ常駐はアダプタとして後段（劣後）
- トークナイザー方式(4.1 の二択)
- embedding モデルの選定(ローカル動作・軽量が条件)
- 名前

## 8. 実装順序

1. トークナイザー(選定方式のミニマム版) + interning
2. コーパスストア(ログ配列 + SA + 世代バッファ)
3. マルコフ生成器(素朴バックオフ + alias 法)— ここで一度 CLI で会話可能に
4. 選択器(embed リランク + ズレ注入)
5. 話題トラッカー・トリガー辞書
6. 候補プール（トリガー + 検索 + マルコフ + エコー）と反おうむ返しスコア
7. 観察窓（既存指標のゲージ）。アダプタ（伺か / Misskey / 常駐）は劣後
8. Kneser-Ney 差し替え、量子化、SIMD 化(楽しみ枠)
9. 分類器・長期記憶・対話行為別プール(v2)

---

## v0.1 で固定した決定

未決だった項目を、動く実装のために仮置きする。名前もトークナイザーも後から差し替えられるように切ってある。

| 項目 | 決定 | 理由 |
|---|---|---|
| クレート名 | `munou` / 表示名 人工無脳君 | プレースホルダ。ラテン語スタイルの改名は容易 |
| 言語 / 版 | Rust 2021, MSRV 1.85（v0.1.14 で依存更新に伴い引き上げ） | edition 2024 は後で |
| インターフェース | `munou-engine` + `munou` CLI。観察窓（`munou observe` / `/observe`）が本線 | 設計 2.1。伺か / Misskey / 常駐は後段 |
| トークナイザー | ゼロ設計: 双方向分岐エントロピー。弱いモデル時は CJK を文字単位 | 「閉じている」と最も整合。文節の代わりに統計チャンク |
| embedding | 閉じた特徴ハッシュ (char n-gram、次数に比例した重み → dim 256)。`Embedder` trait | 単グラムがベクトルを占めないようにした |
| 平滑化 | `Smoothing` trait。既定は Witten-Bell。`--smoothing kn` は modified KN（コーパスの bigram 頻度から D1/D2/D3+）。次数は短い順に補間。KN 経路は PPM-C 除外 | 長い n-gram だけ見て unigram に飛ばすと中間文脈が死ぬ。既製 LM は入れない |
| 再現性 | `ChaCha8Rng` + シード。壁時計はログの `t` にしか使わない。**一様サンプリングは v0.1.14 から自前実装**（`rng.rs`: f64 は上位53bit、整数は Lemire 棄却法）で、契約は ChaCha8 の生ストリーム（仕様固定）と自前の2関数にのみ係留。bot レコードは応答後の word offset を保存し、再開時に同じ位置へ戻す。外部 crate の分布実装変更では応答列が割れない | |
| 耐障害 | データディレクトリ内の `log.jsonl` を append + `sync_data`。インデックスはログから再構築 | mmap コールドスタートは未実装 |
| 評価 | 帯域ヒット率 + 既存発話との連続部分列。`/eval`。帯域は選択器のヒンジにも使う | 面白さの定式化は open のまま。ヒンジは区間制約の凸緩和 |
| 候補 | 既定は `pool`。トリガー・検索・マルコフ・エコーが同時に提案し、選択器が選ぶ | XOR は `--mix exclusive`。対話行為プールはまだ |
| 学習 | 既定 `p_learn=0.35`。会話ログは append-only、コーパス（SA / 検索 / トークナイザ統計）へは乱数で吸収。フラグは JSONL の `learned` | 毎回覚えるとおうむ返しに寄る。シードログはフィールド省略＝吸収済み |
| 観察 | `Observe`。stage は発話・吸収・token 数。ゲージは吸収率・語彙・帯域ヒット・(1−rote)・slip。ボット行に `path` / `novelty_lcs` / `n_tok` を残し再開後も経路と eval が復元。記憶は作業（話題窓）/ 挿話（ログ）/ 母数（SA・語彙）の三層表示 | タマゴッチ型の感情モデルは足さない。HTML はローカルダンプのみ（サーバなし） |
| 逆輸入 | LLM スタックの語彙だけ借りる。nucleus / 経路ゲート / 記憶層 / `/good` `/bad` 事前。生成は接尾辞配列のまま | 外部 API・モデルハブ・事前学習重み・外部ツール接続を製品にしない |
| 規模 | `munou fabricate` / `munou scale` がシードと同じ雑談型を組み合わせて発話を増やす。検索は直近 `n_retrieve_scan=1024`（0 で全件） | Wikipedia 等の外部コーパスは使わない。`data/seed.jsonl` の 50 件は変えない |

実装順序 1–7 と、KN の差し替え口までが入っている。mmap / i8 量子化 / SIMD / 対話行為分類器 / 固有記憶はまだない。伺か SHIORI / Misskey / 常駐は意図的に後回し。

v0.1 を設計書に突き合わせた記録は [`docs/verify.md`](verify.md)。再現は `munou verify`。

## v0.1.5 LLM エコシステムの逆輸入

LLM は使わない。エコシステム（API、Hub、重み、チャット製品）も直接は使わない。借りるのは**概念の形**だけ。閉じたデータ構造に写す。

| LLM 側の語 | munou の写し | 使っていないもの |
|---|---|---|
| temperature / top-p / logprobs | `τ_gen`・`p_nucleus`（既定 1＝無効）・`k_top`・`GenStep.p`/`logp` | モデルロジット、API の logprobs |
| KV cache | `generate_one` 内の `next_counts` メモ | Transformer のキー値キャッシュ |
| MoE / RAG vs weights | `route::plan` が検索枠とマルコフ枠を Cosine とストア占有から配分 | ニューラルルータ、外部検索 API |
| MemGPT 記憶層 | 観察窓: 作業＝話題窓、挿話＝JSONL、母数＝SA/語彙 | ベクトル DB、クラウドメモリ |
| RLHF / DPO | `/good` `/bad` → `path_prior[4]` を選択スコアへ加算。JSONL `role=meta` | 報酬モデル、選好データセット |
| tool / MoE trace | `Trace.route` を `/why` に出す | function calling、外部ツール |

既定の `p_nucleus=1` は次数補間の裾を落とさないため。`--top-p 0.9` で nucleus を足せる。好み行はコーパスに入らない。シード 50 発話は変わらない。

## v0.1.6 学習ログを膨大にする

外部コーパスは使わない。増やすのはシードと同じ生活ログの型（散歩・コーヒー・猫・仕事・ゲーム・挨拶）だけ。`unique_frac=0` なら語彙はほぼ増えず、発話数と SA 長だけが伸びる。それが「同じ人と長く話した」に近い。

検索は全 bot 発話の埋め込みを毎ターン取ると O(N) になるので、既定は直近 1024 件だけ見る。`--retrieve-scan 0` で全件に戻せる。

## v0.1.7 古典 LM の逆輸入

既製の LM ツールも埋め込み学習器も PPM 圧縮器も入れない。借りるのは次数と割引の形だけ。生成器は接尾辞配列のまま。

| 古典 LM 側の語 | munou の写し | 使っていないもの |
|---|---|---|
| Modified Kneser-Ney | `--smoothing kn`。bigram の count-of-counts から Chen-Goodman の D1/D2/D3+。λ = (n1·D1+n2·D2+n3+·D3)/N | 既製 LM ツールチェーン、ARPA ファイル |
| トライ+probing 型 LM 実装 | SA がトライ相当。`NextMemo` が probing 相当。割引は上の MKN | 専用クレート、前処理ツール、mmap バイナリ |
| 可変長 n-gram / PPM / context tree | SA が任意長文脈。短い順に補間。KN 経路は PPM-C 除外（長い次数で見た型を下位から除く）。`--ppm` で Witten-Bell にも除外 | PPM* 圧縮、明示的な context-tree ノード |
| Skip-gram cache LM | 疎な文脈だけ、直前を一つ飛ばした次数と直近 12 トークンの unigram cache を混ぜる（`lambda_skip` / `lambda_cache`） | ニューラル skip-gram / cache |

既定の平滑化は Witten-Bell。最長一致が `f_min` 以上なら skip/cache は混ぜない（丸暗記テストを壊さない）。`kn_discount=0` は割引オフ（soak のアブレーション）。シード 50 発話は変わらない。

## v0.1.15 体験層 — 実製品からの翻訳輸入

解析した既存の非LLM対話製品（返答部は確率テーブル型の無脳）から、**機構だけ**を munou の三法
（閉世界・決定的再現・説明可能）に通る形に翻訳して移した。用語も実装もこちらの体系で作り直して
いる。原典のデータ・セリフ・定数は持ち込まない。

| 箱 | 中身 | 決定性の根拠 |
|---|---|---|
| 関心 (`interest.rs`) | チャンクごとに 熱（半減期 300 トークン）＋根（半減期 10,000 トークン）。選択スコアに `interest_weight × max(チャンク関心)` を加点 | 減衰時計が**ログ位置**（吸収トークン数）。壁時計を見ない |
| 聞きかじり | 2 発話未満（`hearsay_min`）の語は関心 0・アンカー禁止。ターン頭の 1 ロールで「口をつく」(`hearsay_release`) と解禁 | ロールは ChaCha8 ストリーム。発話内の同語は 1 回だけ数える |
| 日和 (`weather.rs`) | なぎ 49% / はずみ・しめり・きまぐれ 各 17%。slip・合いの手・口をつく・気になる語の係数を日替わり変調。特殊日和は 2 日続けない（知覚上のストリークはバグに見える） | 「日」は**前のログレコードの t** の UTC 日。SplitMix64(seed, day) の一発ハッシュで、応答ストリームとは独立 |
| 気になる語 | その日の確立済み語彙から 1 語をハッシュで選び、含む候補に `care_bonus` | 表層文字列でソートしてから選ぶ（トークン id は再開でドリフトするが文字列はしない） |
| 合いの手 (`interject.rs`) | 学習済みの短発話（≤8 文字）を頻度つきで収穫し、`interject_rate`（日和係数つき）で返答の前に一行出す。トリガー同様、自己反復ペナルティの対象外 | 表示のみ: ログに書かず吸収もしないので replay は不変。乱数はロール 1 ＋当選時ピック 1 の固定消費 |
| 節目 / 初語 / よそよそしさ (`milestone.rs`) | 吸収数 {10,100,1k,10k,100k}・日数 {7,30,100,365,…} の**digest 交差**で 1 行。7 日以上の空白で 20 発話ぶんよそよそしく（合いの手 0・気になる語 0・slip 半減）＋「N 日ぶり」 | すべて隣接レコードの性質なので、再生では再発火せず、再開で失われもしない |
| あゆみ (`/ayumi`) | うまれた日・初語・節目・きょうの日和・関心上位 5 語の一枚記録 | 全行がログ由来。感情モデルなし（観察窓と同じ規律） |

新パラメータ: `weather`(on) / `interject_rate`(0.30) / `interest_weight`(0.08) / `care_bonus`(0.05) /
`hearsay_min`(2) / `hearsay_release`(0.15)。CLI は `--no-weather` `--interject-rate` `--interest-weight`。

乱数消費順の契約（v0.1.15 以降）: **口をつくロール → 各ソースの生成 → slip ロール → 学習ロール →
合いの手ロール（バンク稼働時のみ）→ 合いの手ピック（当選時のみ）**。この追加により応答列は全面的に
変わる（宣言済み）。再現契約そのものは不変: 応答は (log, seed, input) の純関数で、日和の「日」も
ログ由来なので、同一ログ＋同一シードの再生は完全一致する。

## Web 面（GitHub Pages）

CLI と同じエンジン crate を wasm32 に落とし、静的ページ 1 枚で対話する面。エンジンは無変更の
方針で、注入点だけ足した: 時計（wasm に SystemTime が無いため `set_now_ms` をターン毎に
JS `Date.now()` から注入 — 日和・節目がブラウザでも動く）、ログ（`open_from_text` /
`export_log` で JSONL 文字列を出し入れし、永続化は端末の localStorage のみ）。シムは
`web/`（ワークスペース外・生 ABI・wasm-bindgen 不使用）で、ルートの依存 allowlist は不変。
再現契約はそのまま: 保存ログの再生は同一状態に戻る（bot 再トークナイズ由来の語彙ドリフトは
既知の非対称のまま）。配信は `.github/workflows/pages.yml`。
